apiVersion: v1
kind: Service
metadata:
  name: api-gateway
  annotations:
    prometheus.io/scrape: "true"  # For monitoring
spec:
  type: NodePort  # Changed from LoadBalancer
  ports:
  - name: http
    port: 80
    targetPort: 8888
    protocol: TCP
  selector:
    app: api-gateway